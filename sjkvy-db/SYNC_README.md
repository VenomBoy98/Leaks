# Repository transfer rule
The 4 migrations + tests are FROZEN artifacts. They must be transferred byte-exact
(zip upload), NEVER regenerated by any AI agent. After extracting sjkvy-db.zip into
the Replit workspace root, verify integrity:  python3 verify_manifest.py
All 26+ files must report OK against MANIFEST.sha256 before any database work.
